import React from "react";
import "./loader.css";

export default function Loader_LAWS() {
  return (
    <div
      style={{
        background: "white",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <div class="loader2"></div>
    </div>
  );
}
